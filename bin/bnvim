#!/bin/sh
# ------------------------------------------------------------------------------
# Build neovim
# ------------------------------------------------------------------------------
cd ~/git/neovim \
  && git pull \
  && sudo make install \
  && echo "Success: neovim built." \
  || echo "Error: neovim did not build."
