#!/bin/bash
# ------------------------------------------------------------------------------
# A check for the host hame.
# ------------------------------------------------------------------------------
hostName() {
  [[ ${HOSTNAME} =~ "$1" ]]
}
hostName "$@"
# ------------------------------------------------------------------------------
