#!/bin/bash
# {{{ check state for all repo types 

checkRepos() {
  echo 'Checking bb'   && gits bb
  echo 'Checking git'  && gits git
  echo 'Checking soup' && gits soup
}

# -------------------------------------------------------------------------  }}}
# {{{ Kick start the show.

checkRepos "$@"

# -------------------------------------------------------------------------- }}}
