#!/bin/bash
# ------------------------------------------------------------------------------
# A check for the existence of a file.
# ------------------------------------------------------------------------------
fileExists() {
  [[ -f "$1" ]]
}
fileExists "$@"
# ------------------------------------------------------------------------------
