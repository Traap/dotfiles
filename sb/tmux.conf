# Generated by bindings/generate-session-bindings; do not edit.

if-shell '[ -z "$SESSION_BINDINGS_HOME" ]' \
  'display-message "WARNING: SESSION_BINDINGS_HOME is not defined."'

set-environment -g HYPRLAND_INSTANCE_SIGNATURE "$HYPRLAND_INSTANCE_SIGNATURE"

if-shell '[ -z "$HYPRLAND_INSTANCE_SIGNATURE" ]' {
  # Alt-lowercase → launch or attach.
  bind-key -n M-d run-shell "ao DataRunner"
  bind-key -n M-n run-shell "ao Neovim"
  bind-key -n M-p run-shell "ao Paperboy"
  bind-key -n M-u run-shell "ao Upgrade"
  bind-key -n M-w run-shell "ao Work"
  bind-key -n M-y run-shell "ao YouTube"
  bind-key -n M-z run-shell "ao Zero"

  # Alt-uppercase → kill.
  bind-key -n M-D kill-session -t DataRunner
  bind-key -n M-N kill-session -t Neovim
  bind-key -n M-P kill-session -t Paperboy
  bind-key -n M-U kill-session -t Upgrade
  bind-key -n M-W kill-session -t Work
  bind-key -n M-Y kill-session -t YouTube
  bind-key -n M-Z kill-session -t Zero
}
