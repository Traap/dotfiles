#!/usr/bin/env bash
# 97-thinkpad-support — Omarchy ThinkPad setup (sudo-safe, idempotent, non-fatal)
set -euo pipefail

# ---------------------------------------------------------------------------
# Flag check
# ---------------------------------------------------------------------------
if [[ ${sqlFlag:-false} != true ]]; then
  log_warn "MS SQL support skipped."
  return
else
  log_info "Installing MS SQL support."
fi

sudo pacman -S --needed unixodbc freetds neovim

# Then from AUR:
# yay -S msodbcsql mssql-tools    # (choose version you need)

# (If you also want to host SQL Server locally)
# yay -S mssql-server

# If you want the GUI:
# yay -S azuredatastudio-bin
