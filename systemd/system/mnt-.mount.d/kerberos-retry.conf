[Unit]
# Desktop indexers may access CIFS automounts before the user's Kerberos
# ticket exists.  Do not permanently rate-limit subsequent mount attempts;
# a later access can then succeed after ad-connect.service obtains a ticket.
StartLimitIntervalSec=0
